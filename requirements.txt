# Core dependencies for the Smart Gate desktop app.
# UI
PySide6==6.7.2
# Camera + image processing
opencv-python==4.10.0.84
numpy==1.26.4
# Backend API client
requests>=2.32,<3
# ALPR: plate detection (ONNX) + OCR (PaddleOCR 2.x API)
onnxruntime==1.24.4
# paddlepaddle 2.6.2 imports setuptools at import time (not bundled with Python 3.12 venvs)
setuptools<81
paddlepaddle==2.6.2
paddleocr==2.10.0
# Staff face attendance (Task 4). dlib has no cp312 wheel — it compiles from
# source (~5-10 min) and needs cmake + g++. A missing face stack must degrade to
# "attendance unavailable", so face_recognition is imported lazily everywhere.
face-recognition==1.3.0
face_recognition_models==0.3.0
dlib==20.0.0
# Offline text-to-speech for the attendance voice notice
pyttsx3==2.90
# Testing
pytest==8.3.2
